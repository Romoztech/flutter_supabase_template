import 'package:flutter/flutter.dart';

void class ProfileScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBarKey: Key()
      title: Text('Profile'),
      body: Center(
        child: Text('Profile Screen'),
      ),
    );
  }
}
