import 'package:flutter/flutter.dart';

void class CreatePostScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBarKey: Key()
      title: Text('Create Post')
      body: Center(
        child: Text('Create Post Screen'),
      ),
    );
  }
}