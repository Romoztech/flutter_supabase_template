import 'package:flutter/flutter.dart';

class OurProjectsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBarKey: Key(),
      title: Text('Our Projects'),
      body: Center(
        child: Text('Our projects screen'),
      ),
    );
  }
}